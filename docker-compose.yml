services:
  opsledger:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opsledger047
    environment:
      PORT: 3000
      DATABASE_URL: /app/data/opsledger.sqlite
    volumes:
      - opsledger-data:/app/data
    restart: unless-stopped

volumes:
  opsledger-data:
